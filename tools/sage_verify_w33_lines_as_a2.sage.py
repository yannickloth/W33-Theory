#!/usr/bin/env sage
"""
Sage cross-check: W33 lines form A2 subsystems under the E8 root↔edge bridge.

Verifies (independently of numpy code):
  - W33 has 40 “lines” (K4 cliques via common-neighbor completion), each with 6 edges.
  - Using artifacts/e8_root_metadata_table.json (root_orbit = E8 simple-root coeffs),
    each line’s 6 roots have pairwise inner products:
      {-2:3, -1:6, +1:6}.
  - For any two distinct lines, the 36 cross inner products are always either:
      (36,0,0) or (12,12,12) for counts of (ip=0, ip=-1, ip=+1).
  - The “commutation graph” on lines (edge iff all 36 cross ips are 0) is SRG(40,12,2,4).

Outputs:
  - artifacts/sage_verify_w33_lines_as_a2.json
  - artifacts/sage_verify_w33_lines_as_a2.md
"""


# This file was *autogenerated* from the file tools/sage_verify_w33_lines_as_a2.sage
from sage.all_cmdline import *  # import sage library

_sage_const_240 = Integer(240)
_sage_const_0 = Integer(0)
_sage_const_1 = Integer(1)
_sage_const_2 = Integer(2)
_sage_const_4 = Integer(4)
_sage_const_3 = Integer(3)
_sage_const_40 = Integer(40)
_sage_const_6 = Integer(6)
_sage_const_8 = Integer(8)
_sage_const_36 = Integer(36)
_sage_const_12 = Integer(12)
_sage_const_540 = Integer(540)
import json
import os
from collections import Counter, defaultdict
from datetime import datetime
from itertools import combinations

from sage.all import *

ROOT_JSON = "artifacts/e8_root_metadata_table.json"
OUT_JSON = "artifacts/sage_verify_w33_lines_as_a2.json"
OUT_MD = "artifacts/sage_verify_w33_lines_as_a2.md"


def load_root_by_edge():
    obj = json.load(open(ROOT_JSON, "r"))
    root_by_edge = {}
    for row in obj["rows"]:
        u, v = row["edge"]
        e = (min(u, v), max(u, v))
        r = tuple(int(x) for x in row["root_orbit"])
        root_by_edge[e] = r
    if len(root_by_edge) != _sage_const_240:
        raise RuntimeError("expected 240 edge->root entries")
    return root_by_edge


def main():
    root_by_edge = load_root_by_edge()

    # Build W33 in the SAME vertex labeling used by the repo artifacts:
    # projective points in F3^4, canonicalized by scaling so the first nonzero is 1,
    # discovered in lexicographic order without sorting (matches numpy scripts).
    pts = []
    seen = set()
    for a in [_sage_const_0, _sage_const_1, _sage_const_2]:
        for b in [_sage_const_0, _sage_const_1, _sage_const_2]:
            for c in [_sage_const_0, _sage_const_1, _sage_const_2]:
                for d in [_sage_const_0, _sage_const_1, _sage_const_2]:
                    if a == b == c == d == _sage_const_0:
                        continue
                    v = [a, b, c, d]
                    # normalize
                    for i in range(_sage_const_4):
                        if v[i] != _sage_const_0:
                            inv = (
                                _sage_const_1
                                if v[i] == _sage_const_1
                                else _sage_const_2
                            )
                            v = [(inv * x) % _sage_const_3 for x in v]
                            break
                    t = tuple(v)
                    if t not in seen:
                        seen.add(t)
                        pts.append(t)
    if len(pts) != _sage_const_40:
        raise RuntimeError("expected 40 points")

    def omega(x, y):
        return (
            x[_sage_const_0] * y[_sage_const_2]
            - x[_sage_const_2] * y[_sage_const_0]
            + x[_sage_const_1] * y[_sage_const_3]
            - x[_sage_const_3] * y[_sage_const_1]
        ) % _sage_const_3

    adj = matrix(GF(_sage_const_2), _sage_const_40, _sage_const_40)
    edge_list = []
    for i in range(_sage_const_40):
        for j in range(i + _sage_const_1, _sage_const_40):
            if omega(pts[i], pts[j]) == _sage_const_0:
                adj[i, j] = _sage_const_1
                adj[j, i] = _sage_const_1
                edge_list.append((i, j))
    if len(edge_list) != _sage_const_240:
        raise RuntimeError(f"expected 240 edges; got {len(edge_list)}")

    # Lines via common-neighbor completion: for each edge, two common neighbors => K4
    lines = set()
    for u, v in edge_list:
        common = [k for k in range(_sage_const_40) if adj[u, k] and adj[v, k]]
        if len(common) != _sage_const_2:
            raise RuntimeError(
                "edge should have exactly 2 common neighbors in SRG(40,12,2,4)"
            )
        L = tuple(sorted([u, v, common[_sage_const_0], common[_sage_const_1]]))
        lines.add(L)
    lines = sorted(lines)
    if len(lines) != _sage_const_40:
        raise RuntimeError(f"expected 40 lines; got {len(lines)}")

    # Group edges by line
    edges_by_line = defaultdict(list)
    for u, v in edge_list:
        common = [k for k in range(_sage_const_40) if adj[u, k] and adj[v, k]]
        L = tuple(sorted([u, v, common[_sage_const_0], common[_sage_const_1]]))
        edges_by_line[L].append((u, v))
    if any(len(edges_by_line[L]) != _sage_const_6 for L in lines):
        raise RuntimeError("expected 6 edges per line")

    # E8 Cartan matrix in Sage canonical order; inner product in simple-root coeff basis: v*C*w
    E8 = RootSystem(["E", _sage_const_8])
    C = matrix(ZZ, E8.cartan_type().cartan_matrix())

    def ip(a, b):
        va = vector(ZZ, a)
        vb = vector(ZZ, b)
        return int(va * C * vb)

    # Per-line A2 check
    a2_ok = True
    a2_fail = []
    for L in lines:
        elist = edges_by_line[L]
        roots = [root_by_edge[e] for e in elist]
        norms = set(ip(r, r) for r in roots)
        ips = Counter()
        for i in range(_sage_const_6):
            for j in range(i + _sage_const_1, _sage_const_6):
                ips[ip(roots[i], roots[j])] += _sage_const_1
        if norms != {_sage_const_2} or ips != Counter(
            {
                -_sage_const_2: _sage_const_3,
                -_sage_const_1: _sage_const_6,
                _sage_const_1: _sage_const_6,
            }
        ):
            a2_ok = False
            a2_fail.append(
                {"line": L, "norms": sorted(list(norms)), "ip_counts": dict(ips)}
            )
            break

    # Line-pair patterns + commutation graph
    patterns = Counter()
    m = len(lines)
    comm_edges = []
    for i in range(m):
        for j in range(i + _sage_const_1, m):
            L1, L2 = lines[i], lines[j]
            c = Counter()
            for e1 in edges_by_line[L1]:
                r1 = root_by_edge[e1]
                for e2 in edges_by_line[L2]:
                    r2 = root_by_edge[e2]
                    c[ip(r1, r2)] += _sage_const_1
            patt = (
                c.get(_sage_const_0, _sage_const_0),
                c.get(-_sage_const_1, _sage_const_0),
                c.get(_sage_const_1, _sage_const_0),
            )
            patterns[patt] += _sage_const_1
            if patt == (_sage_const_36, _sage_const_0, _sage_const_0):
                comm_edges.append((i, j))
            elif patt != (_sage_const_12, _sage_const_12, _sage_const_12):
                raise RuntimeError(f"unexpected cross-ip pattern {patt}")

    G = Graph(comm_edges)
    G.add_vertices(range(m))
    degs = G.degree_sequence()
    deg_set = sorted(set(degs))
    # SRG check: degrees, common neighbors
    if deg_set != [_sage_const_12]:
        raise RuntimeError(f"expected 12-regular; got {deg_set}")
    lambdas = set()
    mus = set()
    for i in range(m):
        for j in range(i + _sage_const_1, m):
            cn = len(set(G.neighbors(i)) & set(G.neighbors(j)))
            if G.has_edge(i, j):
                lambdas.add(cn)
            else:
                mus.add(cn)
    srg = {
        "n": int(m),
        "k": _sage_const_12,
        "lambda": int(list(lambdas)[_sage_const_0]),
        "mu": int(list(mus)[_sage_const_0]),
    }

    status = "ok"
    if not a2_ok:
        status = "fail"
    if patterns != Counter(
        {
            (_sage_const_12, _sage_const_12, _sage_const_12): _sage_const_540,
            (_sage_const_36, _sage_const_0, _sage_const_0): _sage_const_240,
        }
    ):
        status = "fail"
    if srg != {
        "n": _sage_const_40,
        "k": _sage_const_12,
        "lambda": _sage_const_2,
        "mu": _sage_const_4,
    }:
        status = "fail"

    report = {
        "status": status,
        "timestamp": datetime.now().isoformat(),
        "counts": {"lines": _sage_const_40, "edges": _sage_const_240},
        "per_line_a2": {
            "ok": a2_ok,
            "first_failure": a2_fail[_sage_const_0] if a2_fail else None,
        },
        "line_pair_patterns": {str(k): int(v) for k, v in patterns.items()},
        "commutation_graph": {"srg": srg, "edge_count": int(G.size())},
    }

    def to_py(o):
        if isinstance(o, Integer):
            return int(o)
        if isinstance(o, (list, tuple)):
            return [to_py(x) for x in o]
        if isinstance(o, dict):
            return {str(k): to_py(v) for k, v in o.items()}
        return o

    os.makedirs("artifacts", exist_ok=True)
    json.dump(to_py(report), open(OUT_JSON, "w"), indent=_sage_const_2, sort_keys=True)

    md = []
    md.append("# Sage verify: W33 lines as A2 subsystems")
    md.append("")
    md.append(f"- status: `{status}`")
    md.append(f"- lines: `{report['counts']['lines']}`")
    md.append(f"- edges: `{report['counts']['edges']}`")
    md.append(f"- A2 per-line ok: `{a2_ok}`")
    md.append(f"- line-pair patterns: `{report['line_pair_patterns']}`")
    md.append(f"- commutation graph SRG: `{srg}`")
    md.append(f"- JSON: `{OUT_JSON}`")
    open(OUT_MD, "w").write("\n".join(md) + "\n")

    print(f"status={status}")
    if status != "ok":
        raise SystemExit(_sage_const_1)


if __name__ == "__main__":
    main()
