# Phase CCXL - CM Packet-Sum Bridge

This note sharpens the new CM root bridge from Phases CCXXXVI-CCXXXIX.

The recent `j`-tower completion gives the exact cube root

- j(-28)^(1/3) = 255.

The previous phase rewrote this as

- 255 = 15 * 17.

The new point is that the same CM root also splits additively into three
already-promoted W(3,3) packets:

- 255 = 196 + 39 + 20.

## Exact q = 3 identity

At q = 3 we have

- 196 = (lambda*Phi6)^2
-  39 = q*Phi3
-  20 = lambda*Phi4.

Therefore

- j(-28)^(1/3) = (lambda*Phi6)^2 + q*Phi3 + lambda*Phi4
-                 = 196 + 39 + 20
-                 = 255.

The family selector is

- (lambda*Phi6)^2 + q*Phi3 + lambda*Phi4 - q*(Phi4/2)*(Phi4+Phi6)
-   = q(q-3)(q-1)(2q^3 - 2q^2 + 3q - 2)/2.

So q = 3 is the unique positive integer point where this packet-sum identity
holds.

## Reading of the three packets

Each summand already has a promoted meaning on the repo surface:

- 196 = dim(G2)^2 / heat-side packet
-  39 = chiral quadratic packet = 20 + 10 + 9
-  20 = full 4D algebraic curvature shell.

So the new CM cube root is not only a product bridge. It is also an exact sum
of the heat packet, the chiral packet, and the curvature shell.

## Best current reading

The arithmetic-to-continuum bridge now has two complementary forms for the same
CM root:

- product form: 255 = 15 * 17
- packet-sum form: 255 = 196 + 39 + 20.

This is stronger than a one-off factorization because it places the same CM root
simultaneously inside

- the heat / zeta packet,
- the chiral curvature packet,
- the full 4D curvature shell.

This does not finish the smooth bridge theorem. But it is a genuine new bridge:
one CM cube root is now assembled exactly from three distinct packets already
present in the continuum-facing W33 hierarchy.