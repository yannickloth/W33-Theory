# Phase CCXLI - CM Exceptional-Chiral Bridge

This note sharpens the new CM root bridge from Phases CCXXXVI-CCXL.

The recent CM `j`-tower completion gives

- j(-28)^(1/3) = 255.

Earlier phases already identified two exact W(3,3) packets

- E = 240  [edge packet / E8 root packet]
- g = 15   [chiral quadratic Weyl block].

The new point is that the same CM cube root also has the exact additive form

- 255 = 240 + 15
-     = E + g.

## Exact q = 3 identity

Using the standard GQ(q,q) edge count

- E = v*k/2 = q*(q+1)^2*(q^2+1)/2

and the g-multiplicity

- g = q*(q^2+1)/2,

we get the family difference

- q*(Phi4/2)*(Phi4+Phi6) - (E + g)
-   = q^2*(q-3)*(q^2+1)/2.

So q = 3 is the unique positive integer point where

- j(-28)^(1/3) = E + g.

At q = 3 this becomes

- 255 = 240 + 15.

## Best current reading

The arithmetic-to-continuum bridge for the new CM root now has three exact
forms already promoted on the repo surface:

- product form: 255 = 15 * 17
- packet-sum form: 255 = 196 + 39 + 20
- exceptional-chiral form: 255 = 240 + 15.

So the same CM cube root now sits simultaneously in

- the chiral/Clifford packet,
- the heat/chiral/curvature packet hierarchy,
- the exceptional finite packet E = 240 together with one chiral Weyl block.

This does not finish the smooth bridge theorem. But it is a genuine new bridge:
the same arithmetic CM root is now locked simultaneously to the exceptional
finite sector and the continuum-facing chiral sector.