Contents of Claude's workspace (mirrored for documentation and GitHub Pages).

- `data/` — diagnostic artifacts and audit results produced during analysis.
- `run_sage.sh` — helper to run Sage/auxiliary scripts (kept for provenance).

This directory is a read‑only mirror of `claude_workspace/` included for convenience; do not treat these as active source files.
