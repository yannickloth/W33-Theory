# Phase CCXLIII - CM Exceptional Product Bridge

This note sharpens the CM-to-continuum bridge by identifying the exceptional
packet `240` as an exact product of promoted CM cube roots.

The current CM `j`-tower already gives

- j(-4)^(1/3) = k = 12
- j(-7)^(1/3) = g = 15
- j(-8)^(1/3) = v/2 = 20.

The exceptional packet on the finite side is

- E = v*k/2 = 240.

## Exact product bridge

At q = 3 we have the exact factorization

- E = k * (v/2)
-   = 12 * 20
-   = 240.

So the exceptional packet is the product of two promoted CM cube roots:

- E = j(-4)^(1/3) * j(-8)^(1/3).

The family selector is the same exact q=3 lock already used in the continuum
notes,

- v/2 - lambda*Phi4 = (q-3)(q^2+1)/2,

so q = 3 is the unique positive integer point where the `j(-8)` cube root is
also the full 4D curvature shell `lambda*Phi4 = 20`.

## Chiral-Clifford form

From the heat-side balance already present on the repo surface,

- E = g * mu^2 = 15 * 16 = 240.

Thus the same exceptional packet has a second exact factorization

- E = j(-7)^(1/3) * Clifford_4.

So the packet `240` sits between the CM root ladder and the continuum-facing
operator ladder in two different ways:

- E = 12 * 20
- E = 15 * 16.

## Best current reading

The arithmetic-to-continuum bridge now contains the exact multiplicative ladder

- 12 = CM root `j(-4)` cube root
- 20 = CM root `j(-8)` cube root = full 4D curvature shell
- 15 = CM root `j(-7)` cube root = chiral quadratic Weyl block
- 16 = Clifford_4 packet
- 240 = exceptional/E8 packet.

This is stronger than a list of separate identities: the same exceptional packet
is now factored both through two CM roots and through the chiral/Clifford
continuum packet.