# Phase CCXLII - CM Heat-Prime Bridge

This note sharpens the CM root bridge from Phases CCXXXVIII-CCXLI.

The recent CM entry gives

- j(-28)^(1/3) = 255.

The previous phase already rewrote this as

- 255 = 240 + 15
-     = E + g.

The new point is that the same root also collapses to

- 255 = 196 + 59.

## Heat packet plus carrier prime

At q = 3 we have

- 196 = (lambda*Phi6)^2
-  59 = v + k + Phi6.

So

- j(-28)^(1/3) = (lambda*Phi6)^2 + (v + k + Phi6)
-                 = 196 + 59
-                 = 255.

The family selector is

- (lambda*Phi6)^2 + (v+k+Phi6) - q*(Phi4/2)*(Phi4+Phi6)
-   = -(q-3)*(q^4 - 5*q^2 + 4)/2.

So q = 3 is the unique positive integer point where this heat-plus-prime form
holds.

## Why 59 is the right packet

The prime packet 59 is itself the exact q = 3 lock between two earlier bridge
sums:

- 59 = 44 + 15
- 59 = 39 + 20.

Here

- 44 = 4*(k-1)        [old bridge packet]
- 15 = g              [CM Weyl-block root]
- 39 = q*Phi3         [chiral quadratic packet]
- 20 = lambda*Phi4    [full curvature shell].

So the same prime packet 59 stitches together the older bridge layer and the
newer chiral-curvature layer.

## Best current reading

The CM root 255 now has four exact promoted forms:

- 255 = 15 * 17
- 255 = 196 + 39 + 20
- 255 = 240 + 15
- 255 = 196 + 59.

This is stronger than a collection of isolated identities. The new form says
that the same CM cube root is assembled from

- the heat packet 196,
- and a single carrier prime 59 that already unifies two other bridge layers.

This does not finish the smooth bridge theorem. But it is a genuine new bridge:
one CM root is now locked simultaneously to the heat packet, the exceptional
packet, the chiral packet, the curvature shell, and the carrier prime 59.