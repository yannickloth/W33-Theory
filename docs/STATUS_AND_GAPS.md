# Status and gaps (what’s computed vs what’s still physics)

This file is a conservative baseline. It does not supersede the live promoted status language on `docs/index.html#current-synthesis`, `docs/index.html#verified`, or `docs/march_2026_frontier_note.md`.

## What’s currently *computationally verified in this repo*

- `E8` root system is constructed explicitly (240 roots).
- A Coxeter element power partitions roots into `40` orbits of size `6` (the W33 vertex set model used here).
- Under `W(E6)`, the 240 roots decompose as `72 + 6×27 + 6×1`.
- Each `27`-orbit carries a Schläfli graph `SRG(27,16,10,8)` with:
  - `72` K6 cliques (“sixes” of mutually skew lines),
  - `36` double-sixes,
  - stabilizer order `1440 = |S6|×2`.
- The “firewall” split inside an `H27` neighborhood yields:
  - `243 = 216 + 27` (kernel edges + “bad edges”),
  - “bad edges” forming nine disjoint 3-cycles (universally over all 40 embeddings).
- A deterministic, dependency-free **27-state dynamics harness** now exists (replacing notebook-only “physics demos”):
  - builds a Hermitian edge-weighted operator on the Schläfli(27) kernel plus the 27 firewall-bad edges,
  - steps via `U = exp(i dt M)` (NumPy eigendecomposition),
  - computes triangle-holonomy entropy and Cartan-commutator norms,
  - and can random-search a “most conserved” Cartan combination.
  - tools: `tools/toe_dynamics.py`, `tools/toe_phase_diagram.py`
- The A2 (= SU(3)) factor orthogonal to E6 is recovered and validated at the **root-addition** level:
  - E6 and A2 subsystems are closed under root addition,
  - A2 roots act as ladders moving between the six 27-orbits in a clean, 27-to-27 pattern.
- The SU(3) family symmetry is now **canonically gauge-fixed** on the full E8→E6×SU3 mixed sector:
  - A combined GF(2) solve simultaneously fixes:
    - mixed-root sign gauges on all **six** 27-orbits (the 3 and the 3̄ triangles),
    - the symmetric 45-term E6 cubic `d_{ijk}` sign tensor,
    - the required SU(3) antisymmetry bits for ordered color-pairs,
    - uniform ladder-constant bits for each singleton-root orbit transition.
  - Tools: `tools/solve_canonical_su3_gauge_and_cubic.py`, `tools/verify_canonical_su3_gauge_and_cubic.py`
  - Artifacts: `artifacts/canonical_su3_gauge_and_cubic.json`, `artifacts/verify_canonical_su3_gauge_and_cubic.json`
- The E6 cubic tensor support is verified and lifted from E8:
  - 45 tritangent-plane triples (meet-graph triangles) on the 27 lines,
  - 270 mixed E8 root triples collapsing to those 45 with multiplicity 6.
- The 45-term cubic sign tensor is now verified **W(E6)-invariant** in a fixed canonical gauge when the correct
  Weyl-action signs `μ` on root vectors are included (invariance up to overall scaling per generator):
  - tool: `tools/verify_e6_cubic_invariance_canonical_gauge.py`
  - artifact: `artifacts/e6_cubic_invariance_canonical_gauge.json`
- The E6 simple generators now have an explicit **signed-permutation action** on a fixed 27-line model, and we can
  list their concrete swapped pairs (“channels”) inside the Schläfli/meet graphs:
  - tool: `tools/export_we6_signed_action_on_27.py`
  - artifact: `artifacts/we6_signed_action_on_27.json`
  - tool: `tools/build_channel_dictionary_from_we6_generators.py`
  - artifact: `artifacts/we6_channel_dictionary.json`
- A consolidated generator-by-generator **selection parity** report is generated by combining:
  - the canonical 45-term cubic sign tensor,
  - the signed generator action on the 27,
  - the concrete swapped-pair channels in the Schläfli/meet graphs.
  - tool: `tools/generate_selection_rules_report.py`
  - artifacts: `artifacts/selection_rules_report.json`, `artifacts/selection_rules_report.md`
- The firewall’s **9 forbidden triangles** (27 bad edges = 9 meet-graph triangles) are now explicitly mapped onto
  the 45 tritangent-plane triads of the Schläfli 27 model, and cross-tabulated against generator selection parity:
  - tool: `tools/map_firewall_bad_triangles_to_cubic_triads.py`
  - artifacts: `artifacts/firewall_bad_triads_mapping.json`, `artifacts/firewall_bad_triads_mapping.md`
- A compact “protected vs flipped” table is generated for each E6 simple generator on the 45 triads, including
  how many of the 9 firewall-forbidden triads land in each class:
  - tool: `tools/generate_physics_facing_selection_table.py`
  - artifacts: `artifacts/physics_selection_table.json`, `artifacts/physics_selection_table.md`
- A deterministic **sign gauge** exists for the E6 cubic tensor when using a fixed E8 lattice cocycle:
  - the induced signs on the 45 tritangent planes are **not all gauge-removable** (no “all +1” gauge),
  - computed split is 23 positive / 22 negative coefficients in that convention.
- The “remaining15” vertices outside a double-six realize **PG(3,2)** canonically via the K6-edge/duad model
  (15 points, 35 lines, each point on 7 lines, total incidences 105).

## What is *not* yet a physical theory (open work)

Even if the group-/graph-level identifications are correct, turning them into an honest “theory of everything” still requires:

- A Lagrangian / dynamics: a principled action (or Hamiltonian), field content, and equations of motion.
- Chirality: a mechanism for left/right asymmetry (representation choice, anomaly constraints).
- Masses and mixing: Yukawa textures, CKM/PMNS structure, hierarchy, and CP violation from a concrete coupling rule.
- Running couplings and scales: RG flow and a match to measured couplings and thresholds.
- Gravity/spacetime: how Lorentzian geometry and GR (or quantum gravity) emerges, with observationally testable signatures.
- Continuum bridge: no fixed finite spectrum can by itself produce a genuine 4D Weyl law, zeta pole, or Seeley-DeWitt singular asymptotic. Closing the bridge requires a real refinement family or an almost-commutative product with a 4D continuum geometry.
- Experimental falsifiability: specific, quantitative predictions (cross sections, decay rates, cosmology) with uncertainties.
- **Dynkin-lock as a proof artifact:** there are promising “rank-6 Cartan from stabilizer” heuristics, but any claim of a
  score-0 recovered E6 Cartan matrix must be backed by a deterministic, reproducible computation (and is not treated as done).

The repo is currently strong on **rigid combinatorics + Lie/finite-geometry correspondences**.
Bridging to physics demands adding **dynamics, scales, and experimentally checkable numbers**.
