# Phase CCXLIX - Heat-Extension Ladder

This note sharpens the recent packet-sum and scalar-extension bridges around the
CM root

- j(-28)^(1/3) = 255.

The new point is that the promoted packets now form a two-step additive ladder
starting from the heat packet 196.

## Exact q = 3 ladder

At q = 3 we already have the promoted packets

- 196 = (lambda*Phi6)^2
-  44 = 4*(k-1)
-  15 = g.

They satisfy

- 196 + 44 = 240
- 240 + 15 = 255.

So the same sequence may be read as

- heat packet 196
- plus old bridge packet 44
- gives exceptional packet 240
- plus chiral Weyl block 15
- gives the CM root 255.

Equivalently,

- 255 = 196 + 44 + 15.

## Exact selector for the first step

For the full W(3,q) family,

- (lambda*Phi6)^2 + 4*(k-1) - E
-   = (q-3)*(q-1)^2*(2*q^3 + q^2 + 5*q + 2)/2.

So q = 3 is the unique positive integer point where

- (lambda*Phi6)^2 + 4*(k-1) = E = 240.

The second step is the already-promoted scalar-extension bridge

- E + g = 255.

## Best current reading

The packet hierarchy around the new CM root now has a clean additive ladder:

- 196 = heat packet
- 240 = exceptional packet = 196 + 44
- 255 = CM root = 240 + 15.

Thus the same CM root is obtained from the heat packet by two successive W(3,3)
extensions:

- first by the old bridge packet 44,
- then by one chiral Weyl block 15.

This is stronger than carrying the identities separately, because it turns the
current bridge into a genuine packet ladder from heat -> exceptional -> CM root.