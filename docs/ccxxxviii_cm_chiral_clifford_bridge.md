# Phase CCXXXVIII - CM Chiral-Clifford Bridge

This note sharpens the new `j`-tower entry

- j(-28) = 255^3

using the continuum-facing chiral packet developed in Phases CCXXX-CCXXXVII.

The recent CM note gives the exact W(3,3) factorization

- 255 = q * (Phi4/2) * (Phi4 + Phi6).

At q = 3 we already have

- g = q*Phi4/2 = 15
- mu^2 + 1 = 17
- Phi4 + Phi6 = 17.

Therefore the same CM cube root becomes

- 255 = g * (mu^2 + 1)
-     = 15 * 17.

## Exact selector

The only nontrivial step is replacing `Phi4 + Phi6` by `mu^2 + 1`. For the
full W(3,q) family,

- (mu^2 + 1) - (Phi4 + Phi6) = q(3 - q).

So q = 3 is the unique positive integer point where

- Phi4 + Phi6 = mu^2 + 1.

At q = 3 the CM cube root is therefore

- 255 = g * (mu^2 + 1).

## Geometric reading

From the continuum-facing packet:

- g = 15 = dim Sym^2(W+) = dim Sym^2(W-)
- mu^2 = 16 = Clifford_4
- mu^2 + 1 = 17 = Clifford_4 + scalar.

So the new CM cube root is exactly

- one chiral quadratic Weyl block 15
- times the packet (Clifford + scalar) = 17.

That is,

- j(-28)^(1/3) = 15 * (16 + 1).

## Best current reading

The arithmetic-to-continuum bridge now has a second exact CM lock:

- j(-8)^(1/3)  = 20 = full 4D curvature shell
- j(-28)^(1/3) = 15 * 17 = chiral quadratic block * (Clifford + scalar).

So the newer CM root is not floating independently of the continuum package. It
lands directly on the chiral/Clifford architecture already singled out by the
W33 spectral-action packets.

This does not finish the smooth bridge theorem. But it is a genuine new bridge:
a second CM cube root is now sitting inside the same chiral-curvature/Clifford
package as the continuum-facing W33 shell.