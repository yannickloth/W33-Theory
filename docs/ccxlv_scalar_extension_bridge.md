# Phase CCXLV - Scalar-Extension Bridge

This note sharpens the CM / exceptional bridge from Phases CCXXXVIII-CCXLI.

The recent CM root bridge identified

- j(-28)^(1/3) = 255 = g * (mu^2 + 1)

while the exceptional packet already has the exact factorization

- E = 240 = g * mu^2.

At q = 3 this gives the clean difference

- 255 - 240 = g = 15.

So the new CM root is exactly a one-scalar extension of the exceptional packet
along the same chiral Weyl block.

## Exact family selector

Because

- g = q*Phi4/2

holds identically in the W(3,q) family, the only nontrivial step is

- (Phi4 + Phi6) - (mu^2 + 1) = q(q-3).

Therefore

- j(-28)^(1/3) - (E + g)
- = g * ((Phi4 + Phi6) - (mu^2 + 1))
- = q^2 * (q^2 + 1) * (q - 3) / 2.

So q = 3 is the unique positive integer point where

- j(-28)^(1/3) = E + g.

## Best current reading

The same CM root now has the exact bridge forms

- 255 = g * (mu^2 + 1)
- 255 = E + g

with

- E = g * mu^2.

Thus the CM root `255` is obtained from the exceptional packet `240` by adding
exactly one more chiral Weyl block `15`. This is stronger than a bare additive
identity because both packets factor through the same chiral block `g = 15`.

This does not finish the smooth bridge theorem. But it is a genuine new bridge:
the new CM root is now identified as the scalar-extension of the exceptional
packet inside the same chiral/Clifford architecture.