# Local weld: K = <g2,g3,g5,g8,g9> acting on the axis-192 torsor

This bundle executes the four requested steps using only the uploaded computation bundles.

## 1) Build subgroup K
Using `sp43_generators_on_e6pairs_36.json`, take generators **g2,g3,g5,g8,g9** (0-based indices in that file).
The subgroup they generate has:

- |K| (acting on SRG36 vertices) = **162**
- Orbit of the base 7-pocket P = [0, 1, 2, 14, 15, 17, 27] has size **54**
- Stabilizer of P inside K has size **3** (it is a **C3**)

So K is a transitive action on 54 pockets with a C3 stabilizer.

## 2) Export its action on the 192 torsor and 2-sheet deck flip
The axis-fixed completion torsor on a pocket has 192 solutions per table-class sheet (enc0/enc1).
For this K:

- **No deck flip occurs** (enc0 stays enc0; enc1 stays enc1).
- The only nontrivial torsor action appears when you traverse a loop that returns to the base pocket; this produces a **pocket-index permutation sigma** in the C3 stabilizer.

Those sigmas are exactly:
- id = (0,1,2,3,4,5,6)
- s  = (1,3,5,0,2,4,6)
- s⁻¹= (3,0,4,1,5,2,6)

and their induced permutations on the 192 torsor are exported in `torsor_right_multiplication_C3.json`.

## 3) Match inside octonion stabilizer 192
Using `pocket_mult_stabilizer_to_octonion_axis192.json`, each sigma corresponds to an explicit octonion axis-stabilizer element r(sigma) (given by a signed permutation of units 1..7).

**Key identity (verified on all 192 torsor elements):**
> sigma acting on pocket indices == **RIGHT multiplication** by r(sigma) on the 192 torsor (when the torsor is identified with the axis-line stabilizer group H).

## 4) The conjugacy (“local weld”)
The torsor identification coming from `torsor_enc0_axis7_192.json` / `torsor_enc1_axis7_192.json` gives a canonical bijection:
- torsor elements ↔ group elements of H (axis-line stabilizer, order 192).

Under this identification, K’s stabilizer C3 is welded to the octonion C3 ⊂ H generated by r(s).

So the local weld is explicit:
- **K_stab(P) ≅ <sigma> ≅ C3**
- **mapped to** **<r(s)> ⊂ H** via sigma ↦ r(sigma)
- and the action is literally **right regular** on the torsor.

See:
- `K_stabilizer_C3_local_weld.json`
- `torsor_right_multiplication_C3.json`
- `K_schreier_edges_cocycle.csv` (the cocycle over the 54-pocket Schreier graph)
