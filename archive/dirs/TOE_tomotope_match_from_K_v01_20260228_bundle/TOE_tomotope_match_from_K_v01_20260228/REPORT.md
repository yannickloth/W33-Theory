# Tomotope ↔ Axis-192 ↔ K-voltage graph (computed comparison)

## 1) What the repo claims about Tomotope (ground truth)
From `maniplex_doc_bundle/Tomotope.txt`:
- **Flag count: 192**
- **Symmetry order: 96**
- **Flag orbits: 2**
and it lists 4 generating permutations p0..p3 on 12 labels.

From `PART_CXV_reye_tomotope.json`:
- tomotope: (V,E,F,cells) = (4,12,16,8), flags=192, symmetry_order=96, flag_orbits=2
- key insight: tomotope edges/faces match the Reye 12₄16₃ incidence (12 edges ↔ 12 points, 16 faces ↔ 16 lines).

## 2) What we computed from the W33/octonion pocket weld
We already built the **axis-line stabilizer** H inside the octonion table stabilizer:
- H has size **192** (same cardinality as Tomotope flags)

And the local weld identifies the **pocket stabilizer C3** with the order-3 element:
- r = stab_index 399 (order 3) in H
- r^-1 = stab_index 246

This is the **triality/rotation** generator we transport on the axis-192 torsor.

## 3) K Schreier graph has exactly 270 directed edges (tomotope D4↔E6 ratio)
From the local-weld bundle:
- K = <g2,g3,g5,g8,g9>, |K|=162
- Pocket orbit size = 54
- Schreier edges = 54×5 = **270**

Repo `PART_CXIV_tomotope_connection.json` records:
- |W(E6)| / |W(D4)| = **270**

So the K-locus is a *compressed coset geometry* whose edge-count matches the Tomotope “E6 over D4” quotient exactly.

## 4) Voltage interpretation
The Z3 cocycle on the Schreier edges is **not exact** (cycle holonomy is nontrivial).
The canonical Z3 voltage cover has:
- 162 vertices (= 54×3)
- and the lifted generators generate a **regular** permutation group of order 162.

So the Z3 cocycle recovers K itself, as a 3-fold lift of the 54-pocket orbit.

## 5) What matches and what does not (yet)
✅ Matches:
- The **192** torsor layer appears on both sides: Tomotope flags = 192; our axis-line torsor has 192 states.
- The **triality C3** is explicit in the local weld as r of order 3.
- The **270** quotient shows up exactly as the Schreier directed edge count.

⚠️ Not a literal equality of the 96-groups:
- The tomotope symmetry group generated by p0..p3 (computed order 96) has element orders only {1,2,3,4}.
- Our axis-line group H has three index-2 subgroups of order 96, but their order spectra differ.
This suggests we are matching at the **flag-torsor / Weyl-size** layer (192, 270, triality), not yet at the specific 12-label symmetry representation.

## Next “breakthrough” step
Build an explicit 192-flag model for the tomotope monodromy (rank-4 maniplex adjacency involutions),
then search for a conjugator between that 192-action and our axis-line H action using:
- order spectra of involutions,
- orbitals/association scheme invariants,
- and triality element alignment.

That would turn this from a numerological match into a literal action-level identification.

